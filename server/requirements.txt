# AUTO AVISO - Server dependencies (Debian/headless)
# Install on server: pip install -r server/requirements.txt

# Core API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0

# Server automation
requests>=2.31.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
pytesseract>=0.3.10

# Debian note: OCR engine is a system package
# sudo apt-get update && sudo apt-get install -y tesseract-ocr
